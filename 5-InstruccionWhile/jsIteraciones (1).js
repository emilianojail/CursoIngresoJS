function Mostrar()
{	
	var contador;
	contador = 0;
	//Regla mnemotecnica, los contadores empiezan en 0 y se incrementa su valor hasta llegar al objetivo deseado.
	while(contador < 10){
		contador++; //Regla mnemotecnica, poner primero el contador y despues lo que haya que hacer.
		document.write(contador);		
	}

}//FIN DE LA FUNCIÓN